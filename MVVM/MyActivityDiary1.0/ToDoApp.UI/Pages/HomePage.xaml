<UserControl x:Class="ToDoApp.UI.Pages.HomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:ToDoApp.UI.Controls"
             mc:Ignorable="d">

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
        <TextBlock Text="LETS GET IT CHAMP!" FontSize="50" FontWeight="Bold" TextAlignment="Center" Margin="0,10"/>

        <TextBlock Text="Ongoing Tasks:" FontSize="20" FontWeight="Bold" Margin="0,20,0,10" />

        <ItemsControl ItemsSource="{Binding TaskItems}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" FontSize="16" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="TEN TEKST BEDZIE PODMIENIANY MOTYWACYJNYMY HASŁAMI"
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,30,0,0"
                   TextAlignment="Center"/>
    </StackPanel>
</UserControl>